apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: jenkins

helmCharts:
  - name: jenkins
    repo: https://charts.jenkins.io
    releaseName: jenkins
    version: 5.8.66
    namespace: jenkins
    valuesFile: values.yaml

resources:
  - namespace.yaml
