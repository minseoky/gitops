# jenkinsci/jenkins/kustomization.yaml
namespace: jenkins

resources:
  - namespace.yaml
  - jenkins-rbac.yaml

helmCharts:
  - name: jenkins
    repo: https://charts.jenkins.io
    version: 5.8.66       # 원하는 버전으로 지정
    releaseName: jenkins
    namespace: jenkins
    valuesFile: values.yaml
